<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-width=1.0">
    <title>Portfolio | Engineering & Visuals</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@700&family=Inter:wght@300;500;700&display=swap" rel="stylesheet">
    
    <style>
        /* Core Variables: Red, Black, White Theme */
        :root {
            --bg-primary: #ffffff;
            --bg-secondary: #f8f9fa;
            --text-main: #000000;
            --text-light: #495057;
            --accent-red: #d90429;
            --accent-hover: #ef233c;
            --font-body: 'Inter', sans-serif;
            --font-logo: 'Caveat', cursive;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            scroll-behavior: smooth;
        }

        body {
            background-color: var(--bg-primary);
            color: var(--text-main);
            font-family: var(--font-body);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 20px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 1px solid #eee;
        }

        .brand-logo {
            font-family: var(--font-logo);
            font-size: 2.5rem;
            color: var(--text-main);
            text-decoration: none;
        }

        .brand-logo span {
            color: var(--accent-red);
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 40px;
        }

        nav ul li a {
            color: var(--text-main);
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: var(--accent-red);
        }

        /* Hero Section */
        header {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 0 10%;
            background: radial-gradient(circle at top right, #fff5f5, transparent 40%), var(--bg-primary);
        }

        header h1 {
            font-size: 4.5rem;
            line-height: 1.1;
            margin-bottom: 20px;
            letter-spacing: -1px;
            opacity: 0;
            transform: translateY(30px);
            animation: slideUp 1s forwards 0.2s;
        }

        header h1 span {
            color: var(--accent-red);
        }

        header p {
            font-size: 1.2rem;
            color: var(--text-light);
            max-width: 600px;
            margin-bottom: 40px;
            opacity: 0;
            transform: translateY(30px);
            animation: slideUp 1s forwards 0.4s;
        }

        .btn-primary {
            display: inline-block;
            padding: 15px 40px;
            background-color: var(--text-main);
            color: var(--bg-primary);
            text-decoration: none;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: 2px solid var(--text-main);
            transition: all 0.3s ease;
            opacity: 0;
            transform: translateY(30px);
            animation: slideUp 1s forwards 0.6s;
            cursor: pointer;
        }

        .btn-primary:hover {
            background-color: var(--accent-red);
            border-color: var(--accent-red);
            color: white;
            box-shadow: 0 10px 20px rgba(217, 4, 41, 0.2);
        }

        /* Sections */
        section {
            padding: 100px 10%;
        }

        .bg-grey {
            background-color: var(--bg-secondary);
        }

        .section-title {
            font-size: 2.5rem;
            margin-bottom: 50px;
            position: relative;
            display: inline-block;
        }

        .section-title::after {
            content: '';
            position: absolute;
            width: 50%;
            height: 4px;
            background-color: var(--accent-red);
            bottom: -10px;
            left: 0;
        }

        /* About Grid */
        .about-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            align-items: center;
        }

        .about-text p {
            font-size: 1.1rem;
            color: var(--text-light);
            margin-bottom: 20px;
        }

        /* Portfolio Grid */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
        }

        .card {
            background: var(--bg-primary);
            border: 1px solid #e9ecef;
            overflow: hidden;
            cursor: pointer;
            transition: transform 0.4s cubic-bezier(0.165, 0.84, 0.44, 1), box-shadow 0.4s ease;
            position: relative;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.08);
            border-color: var(--accent-red);
        }

        .card-img {
            height: 250px;
            background-color: #f1f3f5;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            color: #dee2e6;
            overflow: hidden;
        }

        .card-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s ease;
        }

        .card:hover .card-img img {
            transform: scale(1.05);
        }

        .card-content {
            padding: 30px;
        }

        .card-category {
            color: var(--accent-red);
            font-size: 0.85rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 10px;
        }

        .card-title {
            font-size: 1.4rem;
            font-weight: 700;
        }

        /* Modals (Pop-ups) */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(8px);
            z-index: 2000;
            display: flex;
            justify-content: center;
            align-items: center;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .modal-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .modal-content {
            background: var(--bg-primary);
            width: 90%;
            max-width: 800px;
            padding: 50px;
            border-top: 5px solid var(--accent-red);
            transform: translateY(50px);
            transition: transform 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
            position: relative;
        }

        .modal-overlay.active .modal-content {
            transform: translateY(0);
        }

        .close-btn {
            position: absolute;
            top: 20px;
            right: 30px;
            font-size: 2rem;
            cursor: pointer;
            color: var(--text-light);
            transition: color 0.3s ease;
        }

        .close-btn:hover {
            color: var(--accent-red);
        }

        .modal-body h2 {
            font-size: 2rem;
            margin-bottom: 15px;
        }

        .modal-body .tech-stack {
            color: var(--accent-red);
            font-weight: 500;
            margin-bottom: 20px;
            font-size: 0.9rem;
        }

        /* Scroll Reveal Classes */
        .reveal {
            opacity: 0;
            transform: translateY(40px);
            transition: all 0.8s cubic-bezier(0.5, 0, 0, 1);
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* Footer */
        footer {
            background-color: var(--text-main);
            color: var(--bg-primary);
            text-align: center;
            padding: 40px 10%;
        }

        @keyframes slideUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 768px) {
            header h1 { font-size: 3rem; }
            .about-content { grid-template-columns: 1fr; }
            nav ul { display: none; } /* Simple mobile hiding for brevity */
        }
    </style>
</head>
<body>

    <nav>
        <a href="#" class="brand-logo">Picography<span>.</span></a>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#projects">Projects</a></li>
        </ul>
    </nav>

    <header id="home">
        <h1>Engineering.<br><span>Visuals.</span> Code.</h1>
        <p>A final-year engineering student blending technical precision with creative vision. Specializing in full-stack development, hardware IoT, and professional photography.</p>
        <a href="#projects" class="btn-primary">View Projects</a>
    </header>

    <section id="about" class="bg-grey">
        <div class="reveal">
            <h2 class="section-title">About Me</h2>
            <div class="about-content">
                <div class="about-text">
                    <p>I am an engineering student currently completing my studies at Vel Tech University in Morai, near Avadi. My work lives at the intersection of technical problem-solving and creative media.</p>
                    <p>As a developer, I build full-stack web applications and hardware scripts. As a creative, I am a photographer and video editor, serving on the core team as Photography Secretary for major campus festivals.</p>
                </div>
                <div style="background: var(--text-main); height: 100%; min-height: 300px; display: flex; align-items: center; justify-content: center; color: white;">
                    [Your Profile Image Here]
                </div>
            </div>
        </div>
    </section>

    <section id="projects">
        <div class="reveal">
            <h2 class="section-title">Featured Work</h2>
            <div class="grid">
                
                <div class="card" onclick="openModal('modal-1')">
                    <div class="card-img">
                        <img src="" alt="BLDC Motor Project" style="background: #333;">
                    </div>
                    <div class="card-content">
                        <p class="card-category">Hardware / IoT</p>
                        <h3 class="card-title">IoT Predictive Fault Detection</h3>
                    </div>
                </div>

                <div class="card" onclick="openModal('modal-2')">
                    <div class="card-img">
                        <img src="" alt="LAVAZA Event Photography" style="background: #444;">
                    </div>
                    <div class="card-content">
                        <p class="card-category">Photography & Design</p>
                        <h3 class="card-title">LAVAZA 2026 & Frame It Right</h3>
                    </div>
                </div>

                <div class="card" onclick="openModal('modal-3')">
                    <div class="card-img">
                        <img src="" alt="Web Development" style="background: #555;">
                    </div>
                    <div class="card-content">
                        <p class="card-category">Full-Stack Dev</p>
                        <h3 class="card-title">Responsive E-Commerce Cart</h3>
                    </div>
                </div>

                <div class="card" onclick="openModal('modal-4')">
                    <div class="card-img">
                        <img src="" alt="Prosthetic Hand" style="background: #666;">
                    </div>
                    <div class="card-content">
                        <p class="card-category">Engineering Design</p>
                        <h3 class="card-title">Prosthetic Hand Fabrication</h3>
                    </div>
                </div>

            </div>
        </div>
    </section>

    <footer>
        <p>&copy; 2026. Designed and developed with precision.</p>
    </footer>

    <div id="modal-1" class="modal-overlay" onclick="closeModal(event, 'modal-1')">
        <div class="modal-content">
            <span class="close-btn" onclick="closeModalForce('modal-1')">&times;</span>
            <div class="modal-body">
                <p class="tech-stack">Sensors • Python • IoT Frameworks</p>
                <h2>IoT-Based Predictive Fault Detection on BLDC Motors</h2>
                <p>A final-year engineering project focused on drone reliability. Engineered a full technical script and integrated a sensor suite to predict and detect faults in Brushless DC motors before catastrophic failure occurs during flight.</p>
            </div>
        </div>
    </div>

    <div id="modal-2" class="modal-overlay" onclick="closeModal(event, 'modal-2')">
        <div class="modal-content">
            <span class="close-btn" onclick="closeModalForce('modal-2')">&times;</span>
            <div class="modal-body">
                <p class="tech-stack">Event Photography • Graphic Design • Branding</p>
                <h2>LAVAZA 2026 Core Team</h2>
                <p>Served as the Photography Secretary for the LAVAZA 2025-26 tech-cultural festival. Responsibilities included organizing the "Frame It Right" photography contest for Tantraz Tech Fest, designing promotional posters without countdowns, and documenting high-profile celebrity campus visits.</p>
            </div>
        </div>
    </div>

    <div id="modal-3" class="modal-overlay" onclick="closeModal(event, 'modal-3')">
        <div class="modal-content">
            <span class="close-btn" onclick="closeModalForce('modal-3')">&times;</span>
            <div class="modal-body">
                <p class="tech-stack">PHP • XAMPP • UI/UX Design</p>
                <h2>Responsive E-Commerce Interface</h2>
                <p>Developed a full-stack e-commerce web application featuring a state-persistent shopping cart. Built utilizing PHP and XAMPP to ensure seamless backend database management and a smooth, responsive user interface.</p>
            </div>
        </div>
    </div>

    <div id="modal-4" class="modal-overlay" onclick="closeModal(event, 'modal-4')">
        <div class="modal-content">
            <span class="close-btn" onclick="closeModalForce('modal-4')">&times;</span>
            <div class="modal-body">
                <p class="tech-stack">Hardware Fabrication • Documentation</p>
                <h2>Fabrication of a Prosthetic Hand</h2>
                <p>A minor engineering project focused on the design and physical fabrication of a functional prosthetic hand, tracking rigorous weekly progress reports through the design phases.</p>
            </div>
        </div>
    </div>

    <script>
        // Scroll Reveal Animation Logic
        function reveal() {
            var reveals = document.querySelectorAll(".reveal");
            for (var i = 0; i < reveals.length; i++) {
                var windowHeight = window.innerHeight;
                var elementTop = reveals[i].getBoundingClientRect().top;
                var elementVisible = 100;
                if (elementTop < windowHeight - elementVisible) {
                    reveals[i].classList.add("active");
                }
            }
        }
        window.addEventListener("scroll", reveal);
        reveal(); // Trigger on load

        // Modal Logic
        function openModal(modalId) {
            document.getElementById(modalId).classList.add('active');
            document.body.style.overflow = 'hidden'; // Prevent background scrolling
        }

        // Close when clicking outside the box
        function closeModal(event, modalId) {
            if (event.target.classList.contains('modal-overlay')) {
                document.getElementById(modalId).classList.remove('active');
                document.body.style.overflow = 'auto';
            }
        }

        // Close when clicking the X
        function closeModalForce(modalId) {
            document.getElementById(modalId).classList.remove('active');
            document.body.style.overflow = 'auto';
        }
    </script>
</body>
</html>